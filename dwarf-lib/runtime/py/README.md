# Dwarf Python Runtime Library

This directory contains the Python runtime implementations for Dwarf's standard library types (Option, Result, List, Map, String, Math, I/O).

These files are imported by compiled Dwarf output when `--target py` is used.
