[package]
name = "dwarf-typecheck"
version = "0.1.0"
edition = "2021"

[dependencies]
dwarf-syntax = { path = "../dwarf-syntax" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
dwarf-syntax = { path = "../dwarf-syntax" }
