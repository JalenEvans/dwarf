//! Token types for the Dwarf lexer.

/// The kind of a token — categorizes what the token represents.
#[derive(Debug, Clone, PartialEq)]
pub enum TokenKind {
    // Placeholder — will be filled in Chunk 1.2
    Placeholder,
}
